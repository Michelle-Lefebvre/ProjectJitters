{% extends "master.html.twig" %}

{% block title %}Menu
{% endblock %}
{% block head %}
	{{ parent() }}
	<style type="text/css">
		.important {
			color: #336699;
		}
	</style>
{% endblock %}

{% block content %}


	<div class="container-fluid menu">

<div> menuList is next </div>


        <ul class="menuItem">
		<h1>Coffees </h1>
        {% for c in cafeList %}
            <div>{{c.itemName}} {{c.price}} {{c.priceMed}} {{c.priceLrg}}</div>
        {% endfor %}
        </ul>
		<br>
		
		<ul class="menuItem">
		<h1>Teas </h1>
        {% for t in teaList %}
            <div>{{t.itemName}} {{t.price}} {{t.priceMed}} {{t.priceLrg}}</div>
        {% endfor %}
        </ul>
		<br>

		<ul class="menuItem">
		<h1>Bake </h1>
        {% for b in bakeList %}
            <div>{{b.itemName}} {{b.price}} {{b.priceMed}} {{b.priceLrg}}</div>
        {% endfor %}
		{% for s in sandList %}
            <div>{{s.itemName}} {{s.price}} {{s.priceMed}} {{s.priceLrg}}</div>
        {% endfor %}
        </ul>
  
 </div>



			{% endblock %}