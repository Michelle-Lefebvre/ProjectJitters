<!DOCTYPE html>
<html>
    <head>
            <link rel="stylesheet" href="/styles.css" />
            <title>{% block title %}{% endblock %} - My Webpage</title>
            <div id="centeredContent">{% block head %}{% endblock %}</div>
    </head>
    <body>
        <div id="centeredContent">
        <div id="contentHeader">
            {% if userSession %}
                <span>You are logged in as <b>{{ userSession.name }}</b>
                    You can <a href="/addarticletest">post a new article</a>
                    or <a href="/logout">Logout</a>.</span>
            {% else %}
                <a href="/login">Login</a> or <a href="/register">Register</a>
            {% endif %}
        </div>
        {% block content %}{% endblock %}
        <div id="footer">
            {% block footer %}
                <br>&copy; Copyright 2011 by <a href="http://domain.invalid/">you</a>.
            {% endblock %}
        </div>
        </div>
    </body>
</html>